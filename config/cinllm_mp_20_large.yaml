# out_dir: 'out/cinllm_mp_20_314_old_order'
out_dir: 'out/cinllm_mp_20_318_int_3e4_4w'
eval_interval: 1000  # how often to evaluate against the validation set
eval_iters_train: 1000
log_interval: 50  # how often to print to the console (1 = every iteration)

# whether to always save a checkpoint
always_save_checkpoint: True

# whether to validate with a validation set
validate: True

# dataset: '/data/keqiangyan/cryllm/CryLLM/tokens_mp20_0314' # old pos
dataset: '/data/keqiangyan/cryllm/CryLLM/tokens_mp_20_pint'
#dataset: '/mnt/data/shared/keqiangyan/tokens_mp_20'
gradient_accumulation_steps: 1  # abandoned
batch_size: 64
block_size: 768

# n_layer: 16
# n_head: 16
n_layer: 8
n_head: 8
n_embd: 1024
dropout: 0.1

# learning_rate: 1e-3 will fail, 7e-4 will be unstable after 5000 iter, 3e-4 best for now
learning_rate: 3e-4
decay_lr: True
max_iters: 40000
# max_iters: 35000
lr_decay_iters: 40000
# lr_decay_iters: 35000
min_lr: 3e-5
beta2: 0.99

warmup_iters: 100

dist: True

